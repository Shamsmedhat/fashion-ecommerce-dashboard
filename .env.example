# Base API URL — already includes /api/v1
# Local backend:
VITE_API_URL=http://localhost:3000/api/v1
# Production (Railway):
# VITE_API_URL=https://fashion-e-commerce-production.up.railway.app/api/v1

# Storefront URL — target for on-demand ISR cache revalidation (POST /api/revalidate)
# Local storefront runs on :3001 (next dev/start -p 3001):
VITE_STOREFRONT_URL=http://localhost:3001
